[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyprogarchives"
dynamic = ["version"]
description = "Typed Python client for Prog Archives (progarchives.com)"
readme = "README.md"
license = { text = "Apache-2.0" }
authors = [{ name = "JarbasAi", email = "jarbasai@mailfence.com" }]
requires-python = ">=3.9"
dependencies = ["requests>=2.28", "beautifulsoup4>=4.11"]
keywords = ["progarchives", "progressive-rock", "music", "scraper", "metadata"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Intended Audience :: Developers",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Internet :: WWW/HTTP",
]

[project.optional-dependencies]
stealth = ["curl-cffi"]
test = ["pytest"]

[project.urls]
Homepage = "https://github.com/TigreGotico/pyprogarchives"
Repository = "https://github.com/TigreGotico/pyprogarchives"

[tool.setuptools.packages.find]
where = ["."]
include = ["pyprogarchives*"]

[tool.setuptools.dynamic]
version = {attr = "pyprogarchives.version.__version__"}
